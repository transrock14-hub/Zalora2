# Hostinger Deployment Ignore File
# Files/folders to exclude when uploading to Hostinger

# Dependencies (installed on server via npm install)
node_modules/

# Build outputs (generated on server via npm run build)
.next/
out/
build/
dist/

# Environment files (set in Hostinger panel, not uploaded)
.env
.env.local
.env.development
.env.production
.env.test
.env*.local

# Development files
.vscode/
.idea/
*.swp
*.swo
*~

# Git files (not needed on server)
.git/
.gitignore
.gitattributes

# TypeScript build info
*.tsbuildinfo
next-env.d.ts

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Netlify (not needed for Hostinger)
.netlify/
netlify.toml
netlify/

# Prisma (not using Prisma anymore, but keep migrations folder just in case)
# prisma/migrations/ - Keep this, might be useful for reference
prisma/*.db
prisma/*.db-journal

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# IDE
*.sublime-project
*.sublime-workspace

# Package manager locks (keep package-lock.json, but not yarn.lock if exists)
yarn.lock
pnpm-lock.yaml

# Uploads folder (empty, will be created on server)
# Keep the folder structure but not the files
public/uploads/*
!public/uploads/.gitkeep

# Scripts (development only - but keep them for reference)
# Actually, keep scripts folder - might be useful

# README files (optional - you can keep or remove)
# README.md
